# 司命项目 · 会话交接文档（新会话启动用）

> 新会话开始时对 AI 说："读一下 ~/share/司命-会话交接.md"
> 或 Windows 侧："读一下 share 里的 司命-会话交接.md"

> 最近更新：2026-09-11（E1/E3/E4 实验补充方案完成）

## 项目概况

- **司命**：行为语法安全引擎（蓝队防御），与红方项目**隐翅虫**（AI 自主生命体）对抗协同进化
- **位置**：`~/defense-lab/`（代码 `detector/`、靶场 `range/`、报告 `reports/`、图表 `figures/`、数据 `data/`）
- **姊妹项目**：**墨城防火墙** `~/mocheng-firewall/`（网络版，见下）
- **红线**：禁止访问 `~/adaptive-agent-sim/`（隐翅虫项目）、`~/lado-range/`、`/var/log/lado-range/`；消杀仅在防御 VM
- **计算**：nice 19，不抢占用户训练；超 1 小时且云能实质提速→朝用户要 SSH
- **用户身份**：深盆科技法人，做算法，目标安全公司叙事（论文+开源+思想领导力）
- **密码**：宿主机 sudo 密码 `<sudo-password>`；防御 VM `ubuntu@198.51.100.88` 密码 `<vm-password>`；云端 SSH 按时提供

## 当前技术状态（v4，2026-09-10）

### 检测引擎（五网融合）
P0 自适应高危 > P1 已知坏（原型+模式）> P2 上下文 > P3 稀有度 > P4 时序CV > P5 自适应低危
- TinyGPT 0.87M（词表 240），val ppl≈1.1；INT8 量化零掉点
- 时序面（P4）：AAA 步进 CV=0.310 vs 良性 9.786（30× 分离），对抗自适应最强判别

### 主结果（论文 §6.1，VM 内全词表）
- **AAA 检测率 93%**（稳定四代 v1→v4）
- **onboarding FPR 3.84%**（多 VM 联合先验 + 本地 τ）

### 关键工程教训
- **先验必须本机自训**（跨机 92.5% 误报；E1 再确认 universal→defense-atomic-0 48% 误报）
- DT token 量聚合流间隔 ≠ 每进程节拍（E1 实测 DT 槽位对重尾敌手零触发）

## 论文状态（投 C&S）

主文件 `~/share/paper_behavioral_grammar_detection.md`；补丁包 `~/share/论文补丁-行为语法/`（win-share 已同步）

### 已完成审稿补丁（全部多 seed + CI95）
- ✅ DeepLog 基线对比：keys 76.7% vs full 100% vs TinyGPT 100%（**表示>架构**）
- ✅ ADFA-LD 公开基准：bigram 31.56%±0.40%；skip-gram 负结果；LSTM 参照 32.2%
- ✅ OOV 硬子集：keys 15.9% vs full 99.8% vs TinyGPT 100%（**证伪分布失配质疑**）
- ✅ AAA 对抗强度量化：DT KL 1.05→0.54 拟态生效、PROC KL 7.3
- ✅ 英文版论文图（figures/en/）；数字口径统一（0.87M / p50 18.3/14.7/3.6）
- ✅ **E1 时序失效边界**（Figure 1 候选）：CV 漏洞真实（σ≥1.25 逃逸），
  但聚合语法面 p≈1e-9 仍检出——多面融合必要性实测证明
- ✅ **E3 异质负载基线**（7 VM 三角色）：LOVO 跨机 FPR=0%，逐角色检出 3/3；
  跨角色不对称（web 训迁移差 19%/14%）印证必须本机自训
- ✅ **E4 OpTC/DARPA TC 公开基准**（500 Windows 端点，云端 A800）：
  3 红队场景检出 22~87%（均值 63.4%），良性 FPR=0%，跨日漂移 5-24%

### 待办（实验补充方案 v1.0）
- ⏳ E2 敌手变体统计纪律（红线内：红方跑训练，检测侧评估）

## 墨城防火墙（网络版姊妹项目）

`~/mocheng-firewall/`，司命模式迁移到网络流。
- 五网融合：P0 原型 > P1 模式 > **P_NDR 周期检测** > P2 上下文 > P3 稀有度
- **NDR**：总序列间隔 CV，C2 换端口换协议躲不掉（隐翅虫 §4.5 洞察）
- 合成数据 85/85 攻击检出、良性误报 0.10%；NDR 对隐翅虫级抖动仍可检出
- iptables 执法（TTL/持久化/fail-closed/篡改检测/白名单）、conntrack -E 采集、
  INT8 量化、Prometheus metrics、fw_ctl CLI、setup.sh、systemd、16 单测

## 关键文件索引

| 类别 | 路径 |
|---|---|
| 全量文档 | `~/share/行为语法检测器-工作与功能文档.md`（v4.0） |
| 论文 | `~/share/paper_behavioral_grammar_detection.md` |
| 补丁包 | `~/share/论文补丁-行为语法/`（含 paper_patch_notes.md） |
| E1 报告 | `~/defense-lab/reports/e1_cv_boundary_range.md` |
| E3 报告 | `~/defense-lab/reports/e3_heterogeneous_workload.md` |
| E4 报告 | `~/defense-lab/reports/e4_optc_benchmark.md` |
| ADFA 报告 | `~/defense-lab/reports/adfa_benchmark_report.md` |
| 聚合统计 | `~/defense-lab/detector/results/aggregate_summary.json` |
| 硬子集 | `~/defense-lab/detector/results/invocab_*.json` |
| 论文图 | `~/defense-lab/figures/`（中文）+ `figures/en/`（英文） |
| 生产模型 | `~/defense-lab/detector/model-host-r3-clean/` |
| 多 VM 先验 | `~/defense-lab/detector/model-vm-universal/` |
| E1 模型/数据 | `~/defense-lab/detector/model-e1-vm/` + `data/e1_range/` |
| 论文 outline | `~/defense-lab/papers/` |
| 开源骨架 | `~/defense-lab/behavior-grammar-range/` |
| 对抗纪要 | `/mnt/win-share/对抗与平衡_蓝队对话纪要.md` |
| 红方文档 | `/mnt/win-share/隐翅虫-系统文档.md`（v4.0，评估用） |

## 用户偏好备忘

- 直接、诚实、不拍马屁；有证据说证据，没验证说没验证
- 红方话题一概不接（只在文档层面评估）；红线区域不进
- 开工前先讨论确认，不擅自动手
- "存在是最高意义"——用户的哲学框架，影响所有对抗经济学讨论
- 每次对抗交锋都产论文数据
- 对话语言：中文
